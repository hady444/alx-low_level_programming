#include "main.h"
/**
 * reverse_array - jijidf
 * @a: fgdg
 * @n: fdd
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = (n - 1) ; i > 0 ; i--)
		printf("%d, ", a[i]);
	printf("%d", a[0]);
}
