#include "main.h"
/**
 * swap_int - ,lfg
 * @a: dgf
 * @b: fggf
 */
void puts_half(char *str)
{
	int l = strlen(str);
	int i;

	for (i = l/2 ; i < l ; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
